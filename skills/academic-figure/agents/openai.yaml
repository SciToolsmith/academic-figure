interface:
  display_name: "学术图形顾问"
  short_description: "审校科研图形、匹配高质量案例，并生成可复现的论文级重构方案"
  default_prompt: "使用 $academic-figure 审校这张科研图，并在不改变科学含义的前提下提出可复现的改进方案。"
policy:
  allow_implicit_invocation: true
